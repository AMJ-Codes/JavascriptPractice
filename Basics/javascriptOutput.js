/*  Types of Outputs
    innerHTML / Ex. document.getElementById("demo").innerHTML = 5 + 5 / Outputs '10' wherever <id> is
    document.write() / Ex. document.write(5 + 6) -> should only be used for testing -> erases all other HTML on page
    window.alert() / Ex. window.alert(5 + 6) OR alert(5 + 6)
    console.log() / Ex. console.log(5 + 6)
    window.print() ? Ex. used to print the content of the current window

    var -> declares variable
    let -> declares a block variable
    const -> declares a block variable
    if -> Marks a block statement to be executed on a condition
    switch -> Marks a block variable to be exeucted in a different cases
    for -> Marks for a block of statements to be executed in a loop
    function -> declares a function
    return -> exits a function
    try -> Implements error handling on block statements
    */


